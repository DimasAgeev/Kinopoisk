export enum AppRoute {
  Main = "/",
  Post = "/post",
  Login = "/sign_in",
  Favorites = "/favorites",
  Registration = "/sign_up",
  Activation = "/activate",
  Reset = "/resetPassword",
  Confedential = "/confedential",
}
