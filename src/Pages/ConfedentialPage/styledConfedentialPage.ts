import styled from "@emotion/styled";
export const ConfedentialWrapper = styled.div`
  display: flex;
  max-width: 90%;
  gap: 0.5rem;
  justify-content: start;
  flex-wrap: wrap;
  position: relative;
  color: white;
  overflow: auto;
  font-size: 1rem;
`;
